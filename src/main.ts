import App from './App.svelte';
// import App from './AppDone.svelte';
// import App from './HelloSvelte.svelte';

const app = new App({
  target: document.body,
  props: {},
});

export default app;
